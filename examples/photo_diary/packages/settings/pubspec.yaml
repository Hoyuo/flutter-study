name: settings
description: Settings feature package for Photo Diary app
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # Core package
  core:
    path: ../core

  # State management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

  # Code generation
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0

  # Functional programming
  fpdart: ^1.1.0

  # Local storage
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  freezed: ^3.0.0
  json_serializable: ^6.9.0
  flutter_lints: ^5.0.0
  bloc_test: ^9.1.7
  mocktail: ^1.0.4
